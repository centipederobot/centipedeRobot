# مستند تقسیم کار تیم توسعه "مغز ربات رهبر"

##**پروژه: توسعه سیستم بینایی و تصمیم‌گیری ربات رهبر چندرباتی**

### **اعضای تیم**
1. **علی محمدزاده** - سرگروه / توسعه‌دهنده ارشد
2. **آقای ذبیحی** - توسعه‌دهنده سطح متوسط  
3. **آقای رافعی پو** - توسعه‌دهنده سطح متوسط

---

## 🏗 **معماری تقسیم کار بر اساس کد موجود**

```
┌─────────────────────────────────────────────┐
│            مغز ربات رهبر (Leader Brain)       │
├──────────────┬──────────────┬──────────────┤
│  ماژول ۱     │  ماژول ۲     │  ماژول ۳         │
│  دریافت و     │  پردازش      │  تصمیم‌گیری        │
│  پیش‌پردازش    │  تصویر       │  و کنترل         │
├──────────────┼──────────────┼──────────────┤
│  آقای        │  آقای        │  علی             │
│  رافعی پو     │  ذبیحی       │  محمدزاده          │
└──────────────┴──────────────┴──────────────┘
```

---

## :memo: **جزئیات تقسیم کار**

### ** ماژول ۱: دریافت و پیش‌پردازش تصویر** 
**مسئول: آقای رافعی پو**

####: **وظایف اصلی:**
```python
# 1. مدیریت استریم ویدئو
class StreamManager:
    async def receiver_task(self):  # موجود در کد
        """دریافت فریم‌ها از ESP32"""
        # بهبود اتصال و مدیریت خطاها
        # بافرینگ هوشمند فریم‌ها
        # بازیابی از قطعی اتصال

# 2. پیش‌پردازش تصویر  
class Preprocessor:
    def preprocess_frame(self, frame):
        """آماده‌سازی فریم برای پردازش"""
        # تنظیم سایز (resize)
        # تصحیح رنگ و نور
        # حذف نویز
        # کش کردن فریم‌ها (frame_cache)
```

#### **تحویل‌دادنی‌ها:**
- [ ] کلاس `StreamHandler` با قابلیت اتصال به چند منبع
- [ ] سیستم مدیریت بافر فریم‌ها با اولویت‌دهی
- [ ] توابع پیش‌پردازش تصویر (نرمال‌سازی، فیلترگذاری)
- [ ] گزارش عملکرد استریم (FPS، کیفیت، تأخیر)

#### ⏱️ **زمان پیش‌بینیشده: ۲ هفته**

---

### ** ماژول ۲: پردازش تصویر و تحلیل محیط**  
**مسئول: آقای ذبیحی**

#### :white_check_mark: **وظایف اصلی:**
```python
# 1. پردازش بینایی اصلی
class VisionProcessor:
    async def vision_worker(self):  # موجود در کد
        """محاسبه هزینه مسیرها"""
        # محاسبه Optical Flow
        # تشخیص لبه‌ها (Canny)
        # تحلیل تغییرات نور
        # محاسبه هزینه ۷ مسیر

# 2. تشخیص شرایط محیطی
class EnvironmentAnalyzer:
    def analyze_conditions(self, frame):
        """تشخیص وضعیت‌های خاص"""
        # تشخیص سایه
        # تشخیص راه باریک
        # تشخیص محیط تاریک
        # تشخیص مانع نزدیک
```

####**تحویل‌دادنی‌ها:**
- [ ] کلاس `PathCostCalculator` برای محاسبه هزینه مسیرها
- [ ] ماژول `OpticalFlowAnalyzer` بهبودیافته
- [ ] سیستم تشخیص سایه و تغییرات نور
- [ ] الگوریتم ارزیابی کیفیت مسیرها
- [ ] تست‌های واحد برای پردازش تصویر

#### ⏱️ **زمان پیش‌بینیشده: ۳ هفته**

---

### ** ماژول ۳: تصمیم‌گیری و کنترل**  
**مسئول: علی محمدزاده (سرگروه)**

#### **وظایف اصلی:**
```python
# 1. موتور تصمیم‌گیری
class DecisionEngine:
    async def command_worker(self):  # موجود در کد
        """تصمیم‌گیری بر اساس داده‌های بینایی"""
        # استراتژی‌های ناوبری
        # مدیریت شرایط خاص (گیر کردن، دیوار)
        # اولویت‌دهی دستورات

# 2. سیستم کنترل و هماهنگی آینده
class TeamCoordinator:
    def coordinate_followers(self):
        """هماهنگی با ربات‌های زیردست (آینده)"""
        # طراحی پروتکل ارتباطی
        # تخصیص وظایف
        # مدیریت فرماسیون
```

#### **تحویل‌دادنی‌ها:**
- [ ] کلاس `CommandDecisionMaker` با چندین استراتژی
- [ ] سیستم مدیریت حالت (State Machine) برای ربات
- [ ] ماژول واکنش به شرایط اضطراری
- [ ] طراحی اولیه API برای ارتباط با ربات‌های زیردست
- [ ] مستندات الگوریتم‌های تصمیم‌گیری

#### ⏱️ **زمان پیش‌بینیشده: ۲.۵ هفته**

---

## 🛠 **کارهای تکمیلی مشترک**

### **زیرساخت و ابزارها**
**مسئول: علی محمدزاده** (با مشارکت همه)

1. **سیستم لاگینگ و مانیتورینگ**
   - بهبود `logger_worker`
   - داشبورد مانیتورینگ Real-time
   - ذخیره‌سازی داده‌های آموزشی

2. **پیکربندی و تنظیمات**
   - فایل `config.py` متمرکز
   - سیستم تنظیم خودکار پارامترها
   - مدیریت پروفایل‌های مختلف

3. **آزمون و ارزیابی**
   - تست‌های واحد برای هر ماژول
   - شبیه‌ساز محیط برای آزمایش
   - معیارهای عملکرد (Performance Metrics)

### **زمان‌بندی کلی پروژه**

```
هفته ۱-۲: ماژول ۱ (دریافت تصویر) + زیرساخت
هفته ۲-۴: ماژول ۲ (پردازش تصویر)  
هفته ۴-۵: ماژول ۳ (تصمیم‌گیری) + یکپارچه‌سازی
هفته ۶:   تست، بهینه‌سازی و مستندسازی
```

---
### **ابزارهای تیمی:**
- **GitHub** برای کنترل نسخه (هر نفر روی Branch جدا)
- **Telegram/eitaa** برای ارتباط فوری

---

## **منابع و راهنمایی**

### **برای آقای رافعی پو (ماژول ۱):**
- OpenCV VideoCapture Documentation
- Asyncio Queue Patterns
- ESP32 Camera Streaming Protocols

### **برای آقای ذبیحی (ماژول ۲):**
- Optical Flow Algorithms
- Canny Edge Detection Optimization  
- Image Processing Techniques

###:برای همه
- کد نمونه فعلی (پایه کار)
- مقالات مرتبط با Multi-Robot Systems
- نمونه‌های مشابه GitHub
- هوش مصنوعی
---

## **معیارهای موفقیت**

### **پروژه موفق است اگر:**
1. **ربات بتواند در محیط آزمایشی حرکت کند** (هفته ۴)
2. **درصد موفقیت ناوبری > 85%** (هفته ۵)
3. **کد ماژولار و قابل توسعه باشد**
4. **مستندات کامل و قابل فهم باشد**

---
## **پشتیبانی و کمک**

- **علی محمدزاده** برای مسائل فنی پیچیده
- **جلسات کدنویسی گروهی** برای حل مشکلات مشترک
- **مشارکت در ریویو کدها** برای بهبود کیفیت
---
